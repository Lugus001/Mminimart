import { defineComponent } from "vue";

export default defineComponent({
  name: 'product',
  setup() {
    return () => (
        <nav class="produc">
            <div class=''>

            </div>
        </nav>
    )
  }
});